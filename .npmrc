@hollis-studio:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
# Never resolve without peer deps: lockfiles regenerated with legacy-peer-deps
# silently drop peer entries and break strict `npm ci` in every consumer CI.
legacy-peer-deps=false
